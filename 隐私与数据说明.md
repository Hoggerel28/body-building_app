# 隐私与数据说明

## 敏感数据

训练记录、体重、体脂、饮食、训练复盘和歌单偏好都可能反映个人健康、生活习惯或身份信息。公开分享截图、备份、日志或 APK 前，请先确认其中没有隐私内容。

## 本地保存

当前主要数据保存在浏览器 `localStorage`。换浏览器、清理网站数据、重装系统或卸载 App，都可能导致记录丢失。建议定期在“我的”页面导出 JSON 备份，导出前页面会提示确认。

JSON 备份文件可能包含完整训练、饮食、体重、复盘和个人资料。不要把备份文件随意发送到聊天、网盘、公开仓库或不可信设备。

## Supabase

Supabase 表和 RLS 已准备，但业务数据完整云同步尚未完成。后续接入云同步必须继续遵守：

- 前端只使用公开 `anon` key。
- 不在前端、聊天、截图或仓库放 `service_role`、数据库密码、OAuth AppSecret、token、cookie、Android keystore 或私钥。
- 不关闭 RLS。
- 好友可见复盘必须依赖数据库策略校验，不能只靠前端隐藏。

## Android

Manifest 当前只声明 `INTERNET` 权限，并关闭 Android 系统备份。即便如此，App 内本地数据和手动导出的 JSON 仍需要用户自行妥善保管。

## 音乐链接

音乐页只保存外部平台链接。点击链接会离开当前 App，后续行为受对应音乐平台隐私政策影响。

## 健康建议

App 内训练建议只用于记录和辅助复盘，不是医疗建议。疼痛、疾病、伤病恢复或特殊人群训练应咨询专业人士。
