# Android 打包与安装说明

## 当前项目状态

- Capacitor 已接入，Android 工程目录为 `android/`。
- App 标识为 `com.fittrack.app`。
- App 名称为 `FitTrack`。
- Web 构建目录为 `dist`。
- AndroidManifest 当前只声明了 `INTERNET` 权限。
- Manifest 中已关闭 Android 系统备份：`android:allowBackup="false"`、`android:fullBackupContent="false"`。

## 同步 Android 工程

推荐先执行项目脚本：

```powershell
npm.cmd run cap:sync:android
```

等价于：

```powershell
npm.cmd run build
npx.cmd cap sync android
```

## 生成 Debug APK

补齐本机 Android 环境后，进入 `android/` 执行：

```powershell
.\gradlew.bat assembleDebug
```

成功后 Debug APK 通常在：

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

## 本机环境要求

- 安装 Android Studio。
- 在 Android Studio 中安装 Android SDK 36、Platform Tools 和 Build-Tools。
- 配置 `ANDROID_HOME` 或 `ANDROID_SDK_ROOT` 指向本机 SDK 目录。
- 配置 `JAVA_HOME` 指向 JDK 21；当前 Capacitor Android 工程使用 Java 21 编译目标。
- 首次执行 Gradle 时需要联网下载 wrapper 和依赖。

## 当前已知阻塞

- 主线程已执行 `npm.cmd run build`，构建成功。
- 主线程已执行 `npx.cmd cap sync android`，Web 资源已同步到 Android 工程。
- 当前机器未检测到 `ANDROID_HOME` 和 `ANDROID_SDK_ROOT`。
- 默认 Android SDK 目录 `C:\Users\杨瑞\AppData\Local\Android\Sdk` 不存在。
- 当前 `JAVA_HOME` 指向 JDK 17，但本项目 Android 构建要求 JDK 21。
- Gradle 8.14.3 wrapper 仅有未完成的 `.zip.part` 下载；本次不重复触发长时间下载，也不删除缓存。
- 因以上 Android SDK / JDK / Gradle 环境阻塞，本次未生成 APK。

## 安装提醒

Debug APK 仅适合测试，不适合公开正式分发。对外发送安装包前请先确认对象和用途，并提醒对方注意“未知来源应用”安装风险，以及应用内健康数据和 JSON 备份的隐私风险。
