# 发布说明

## v0.2.0 测试版

本版目标是把原网页 demo 升级成可继续接 Supabase、可继续打包 Android 的测试版。当前仍属于本地优先版本，不应对外承诺完整云同步或正式 APK 分发能力。

## 重点变化

- 移除假默认数据，连续打卡从真实记录计算。
- 手机端展示连续打卡和本周训练次数。
- 减脂 / 增肌增加不同指标、建议和视觉状态。
- UI 重新分块，标题和按钮文案更贴近日常训练场景。
- 肌肉图升级为可交互正反面热区。
- 增加本地搜索、歌单链接、好友圈原型、复盘可见性。
- 增加邮箱注册 / 登录入口。
- 增加 Supabase 表、RLS 和好友请求 RPC 地基。
- 增加 Vite、Capacitor Android 工程。

## 暂未完成

- 训练、饮食、体重、复盘、好友关系和歌单链接尚未完整云同步。
- 微信 / QQ 快捷登录未接入。
- 音乐平台账号歌单同步未接入，只支持保存外部链接。
- APK 尚未稳定产出，可能受 Android SDK、JDK 或 Gradle 下载环境阻塞。
- 尚未配置正式签名、应用商店发布流程和隐私合规材料。

## 安全与隐私提示

- Supabase 前端只应使用 `anon` key，不要使用或泄露 `service_role`、数据库密码、OAuth AppSecret、token、cookie、Android keystore 或私钥。
- 训练、饮食、体重、复盘和 JSON 备份可能包含敏感健康数据，不建议随意转发。

## 本轮主线程验证

- `node --check app.js` 通过。
- `node --check server.js` 通过。
- `npm.cmd run build` 通过。
- `npx.cmd cap sync android` 通过。
- 390x844 移动端 QA 通过：默认连续 0 天，保存训练后连续 1 天，减脂 / 增肌指标可切换，搜索和歌单入口可用。
